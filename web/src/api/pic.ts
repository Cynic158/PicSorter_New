const PicApi = {
  getPicList: window.DeskApi?.Pic_getPicList,
  renamePic: window.DeskApi?.Pic_renamePic,
  getPicInfo: window.DeskApi?.Pic_getPicInfo,
  showPic: window.DeskApi?.Pic_showPic,
};

export default PicApi;
