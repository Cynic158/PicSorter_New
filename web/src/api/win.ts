const WinApi = {
  QUIT: window.DeskApi?.Win_quit,
  HIDE: window.DeskApi?.Win_hide,
  MAX: window.DeskApi?.Win_max,
  COPY: window.DeskApi?.Win_copy,
  LINK: window.DeskApi?.Win_link,
};

export default WinApi;
